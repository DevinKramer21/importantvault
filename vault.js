// Step 1: Display a message to the user
console.log("You have received this message because you have been chosen to open an important vault. Here is the secret combination:");

// Calculate the first code (10) using addition
const code1 = 5 + 5; // Using addition (+) to get 10

// Calculate the second code (40) using multiplication
const code2 = 8 * 5; // Using multiplication (*) to get 40

// Calculate the third code (39) using division
const code3 = 78 / 2; // Using division (/) to get 39

// Display the vault codes and the message to the user
alert(`Vault Combination:\n${code1} - ${code2} - ${code3}`);
